step_name:
  type: build
  title: py_image
  working_directory: ${{clone_step_name}}
  dockerfile: Dockerfile
  image_name: ziv/test
step_name:
  type: composition
  title: Step Title
  description: Free text description
  working_directory: ${{a_clone_step}}
  composition:
    version: '2'
    services:
      web:
        image: ${{build_step}}
      redis:
        image: "redis:alpine"
   composition_candidates: ziv/test
